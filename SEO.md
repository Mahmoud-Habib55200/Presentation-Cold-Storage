# دليل تصدُّر الموقع في محركات البحث

هذا الملف يشرح **ما تم تنفيذه في الكود**، و**ما يجب أن تنفذه أنت** خارج الكود.
الجزء الثاني هو الأهم — الكود وحده لا يُصدِّر موقعًا.

---

## ١) بيانات الموقع الرسمية — مضبوطة بالفعل

| البند | القيمة |
|---|---|
| الدومين | https://www.coolstock.store/ |
| النسخة الإنجليزية | https://www.coolstock.store/en/ |
| بريد التواصل | sales@coolstock.store |
| اسم العلامة | CoolStock |

كل الروابط المطلقة في `index.html` و `en/index.html` و `robots.txt` و `sitemap.xml`
تشير إلى هذا الدومين بالفعل. لا حاجة لأي استبدال يدوي.

> **مهم عند النشر على GitHub Pages:** ملف `CNAME` موجود في جذر المشروع ويحتوي على
> `www.coolstock.store`. بعد أول نشر، فعّل **Enforce HTTPS** من إعدادات المستودع،
> وتأكد أن سجل DNS عند مزوّد الدومين يشير إلى GitHub Pages.

> **وحّد النطاق:** اختر `www` أو بدونها ووجّه الأخرى إليها بتحويل 301. الموقع مضبوط
> حاليًا على `www` — فوجّه `coolstock.store` إلى `www.coolstock.store`، وإلا اعتبرهما
> جوجل موقعين منفصلين وقسّم قوة الدومين بينهما.

---

## ٢) ما تم تنفيذه في الكود

### أساسيات الفهرسة
- عنوان `<title>` مبني على كلمات بحث حقيقية بدل "عرض تقديمي" (الكلمة الأخيرة كانت تُفسد الاستهداف تمامًا).
- `meta description` بطول مناسب لمقتطف نتائج البحث.
- `canonical` لمنع ازدواج المحتوى.
- `robots` مع `max-image-preview:large` — يسمح بصورة كبيرة في نتيجة البحث.
- `robots.txt` و `sitemap.xml` بتاريخ تعديل وصور مفهرسة.

### الاستهداف اللغوي والجغرافي
- وسوم `hreflang` تربط النسخة العربية بالإنجليزية (`ar`, `ar-EG`, `ar-SA`, `en`, `x-default`).
- نسخة إنجليزية كاملة في `en/index.html` بنفس التصميم، تستهدف `cold storage WMS` وما يشبهها.
- `areaServed` في البيانات المنظمة يشمل مصر والسعودية والإمارات والكويت وقطر.

### البيانات المنظمة (Schema.org / JSON-LD)
رسم بياني واحد يضم: `Organization` + `WebSite` + `WebPage` + `SoftwareApplication` + `FAQPage` + `BreadcrumbList`.

`FAQPage` مبني **آليًا من الأسئلة الظاهرة فعلًا على الصفحة** — جوجل يعاقب على البيانات المنظمة
التي لا يراها الزائر، فلا تحذف قسم الأسئلة من الصفحة مع إبقاء الـ schema.

### المحتوى (أهم عامل ترتيب)
الصفحة كانت تعتمد على صور وعناوين قصيرة — وهذا لا يُفهرَس. أُضيف:
- قسم **"مين بيستخدم النظام؟"** بستة أنشطة (خضار وفاكهة، لحوم ودواجن، أدوية، مصانع أغذية،
  تخزين للغير، شركات لوجستية) — هذه تصطاد البحث طويل الذيل.
- قسم **أسئلة شائعة** بتسعة أسئلة وإجابات تفصيلية.
- عناوين `H2` للأقسام التي كانت تبدأ بـ `H3` مباشرة، مع فقرات وصفية.
- فوتر موسّع بروابط داخلية ونص يشرح النشاط والنطاق الجغرافي.

### السرعة (Core Web Vitals)
- كل الصور حُوِّلت إلى WebP: **3.5 MB ← 1.0 MB (أقل بـ 72%)**. ملفات PNG الأصلية باقية كما هي.
- `width` و `height` صريحان على كل صورة — يمنع قفزات التخطيط (CLS).
- `loading="lazy"` لكل الصور عدا صورة الواجهة، التي أخذت `fetchpriority="high"` و `preload`.
- `decoding="async"` على الجميع.

### بحث الصور
نص `alt` وصفي بكلمات مفتاحية لكل صورة بدل نصوص مثل "الخريطة الحية".

---

## ٣) ما يجب أن تنفذه أنت — بالترتيب

### فورًا بعد النشر
1. **Google Search Console** — <https://search.google.com/search-console>
   أضف الدومين، وثّق الملكية، ثم أرسل `sitemap.xml`، ثم استخدم **URL Inspection → Request Indexing**
   للصفحتين. بدون هذه الخطوة قد يتأخر ظهورك أسابيع.
2. **Bing Webmaster Tools** — <https://www.bing.com/webmasters>
   يمكنك استيراد الإعدادات من Search Console بضغطة. مهم لأن Bing يغذّي محركات أخرى.
3. **اختبر البيانات المنظمة** — <https://search.google.com/test/rich-results>
   يجب أن يظهر `FAQPage` و `SoftwareApplication` بدون أخطاء.
4. **قِس السرعة** — <https://pagespeed.web.dev>

### خلال الأسبوع الأول
5. **Google Business Profile** — إن كان لديك عنوان أو نشاط تجاري مسجّل.
   هذا أقوى عامل منفرد للظهور في البحث المحلي (بحث بصيغة "شركة نظام مخازن مبردة في القاهرة").
6. **الدليل التجاري:** أضف الشركة في أدلة الأعمال العربية والخليجية المتخصصة في اللوجستيات
   وسلاسل التبريد. كل ظهور بذكر اسم الشركة + رابط يبني ثقة الدومين.
7. **LinkedIn**: صفحة شركة + منشور يشرح النظام برابط الموقع.

### مستمر — وهذا ما يصنع الفرق فعلًا
8. **مدوّنة.** الصفحة الواحدة تستطيع الترتيب على عدد محدود من الكلمات فقط. أنشئ `/blog/`
   واكتب مقالات تجيب أسئلة حقيقية يبحث عنها عملاؤك:
   - "الفرق بين FIFO و FEFO في المخازن المبردة"
   - "كيف تحسب تكلفة تخزين الباليتة في الثلاجة"
   - "درجات الحرارة المثالية لتخزين الخضار والفاكهة" (جدول — يُصطاد كـ featured snippet)
   - "أخطاء شائعة في إدارة الثلاجات تسبب هلاك البضاعة"

   مقال واحد جيد أسبوعيًا يتفوق على أي تحسين تقني.

9. **صفحات هبوط منفصلة لكل نشاط.** حاليًا "مين بيستخدمه" قسم داخل الصفحة. حوّل كل بطاقة
   إلى صفحة مستقلة (`/cold-storage-vegetables/`, `/pharma-warehouse/`) عندما يكون لديك
   محتوى كافٍ لكل واحدة — كل صفحة ترتب على كلماتها الخاصة.

10. **روابط خارجية (Backlinks).** أقوى عامل خارج موقعك. اكتب مقالًا ضيفًا في موقع لوجستيات
    عربي، شارك في تقرير قطاعي، أو ارعَ فعالية بذكر رابطك. جودة الرابط أهم من عدده بكثير.

11. **راجع Search Console شهريًا.** تقرير **Performance** يُظهر الكلمات التي تُعرض عليها فعلًا.
    الكلمة التي تظهر في المركز ١١–٢٠ هي أسرع مكسب — حسّن محتواها لتقفز للصفحة الأولى.

---

## ٤) توقعات واقعية

| المدة | المتوقع |
|---|---|
| أسبوع ١–٢ | فهرسة الصفحتين وظهورهما عند البحث باسم العلامة |
| شهر ١–٣ | ترتيب على كلمات طويلة الذيل منخفضة المنافسة |
| شهر ٣–٦ | تحسن على الكلمات المتوسطة — **بشرط نشر محتوى منتظم** |
| شهر ٦–١٢ | منافسة على الكلمات الرئيسية مثل "نظام إدارة المخازن المبردة" |

السيو التقني الذي نُفِّذ هنا يضع الأساس ويزيل العوائق. لكن الترتيب على الكلمات
التنافسية يأتي من **المحتوى + الروابط + الوقت** — لا يوجد اختصار لهذا الجزء.

---

## ٥) نقاط تستحق الانتباه

- **البريد الإلكتروني** (sales@coolstock.store) هو وسيلة التواصل الوحيدة حاليًا. إضافة **رقم هاتف**
  يرفع معدل التحويل بوضوح، ويسمح بإضافة `telephone` في بيانات `Organization` المنظمة.
- **العنوان الفعلي** غير مذكور. إن كان لديك مقر، أضفه — يفتح لك الباب لبيانات `LocalBusiness`
  المنظمة وللبحث المحلي، وهما مكسب كبير لنشاط بهذا الطابع.
- **السعر** مضبوط حاليًا على `0` في `Offer` بمعنى "عرض تجريبي مجاني". إن أعلنت تسعيرًا حقيقيًا،
  حدّثه — التسعير المعلن يزيد ظهور المقتطفات الغنية.
- **لقطات شاشة التطبيق ما زالت تحمل الاسم القديم.** الصور في `assets/` مأخوذة من النظام وهو
  يعرض "سيستم التلاجات" في شريطه الجانبي. الموقع كله صار CoolStock، لكن الصور نفسها لم تتغير —
  وهي تظهر للزائر وفي صورة المشاركة الاجتماعية. صدِّر لقطات جديدة من النظام بعد تحديث اسم
  العلامة داخله، ثم أعد تشغيل تحويل WebP.

- لا تحذف قسم الأسئلة الشائعة من الصفحة دون حذف `FAQPage` من البيانات المنظمة.
